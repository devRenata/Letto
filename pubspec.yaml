name: letto
description: "Aplicativo de moradia colaborativa que conecta pessoas com perfis compatíveis para compartilhar aluguel de forma prática, acessível e segura."
version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
